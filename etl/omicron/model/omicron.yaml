---
table:
  columns:
    date:
      type: DATE
      primary_key: true
    location:
      type: VARCHAR(50)
      primary_key: true
      index: true
    variant:
      type: VARCHAR(50)
      primary_key: true
      index: true
    num_sequences:
      type: DECIMAL(13,0)
    perc_sequences:
      type: DECIMAL(15,2)
    num_sequences_total:
      type: DECIMAL(13,0)
process:
  materialization: table
  mode: drop

