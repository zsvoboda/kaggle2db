---
table:
  columns:
    date:
      type: DATE
    location:
      type: VARCHAR(25)
    variant:
      type: VARCHAR(25)
    num_sequences:
      type: DECIMAL(13,0)
    num_sequences:
      type: DECIMAL(15,2)
    num_sequences_total:
      type: DECIMAL(13,0)
process:
  materialization: table
  mode: drop

